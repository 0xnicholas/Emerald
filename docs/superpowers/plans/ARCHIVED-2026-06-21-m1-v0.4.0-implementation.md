# M1 (v0.4.0) 实施计划：部署与验证基础设施

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 Emerald 从「能跑」升级到「能部署到生产并被验证」——完成 Dockerfile 优化、K8s 灾备演练、OpenTelemetry 分布式追踪完善、真实 LLM 基准跑分、基准 CI 自动化 5 个工作项。

**Architecture:**
- M1 是路线图 v0.4.0 的全部内容（详见 `docs/roadmap.md` §四 M1）
- 5 个工作项中：A1→A2 是关键路径，A4 独立，B0→D1 是另一条关键路径
- 每个工作项独立可验证、可回滚
- 所有变更必须保持现有 601 个测试通过

**Tech Stack:**
- Docker multi-stage build (Dockerfile)
- Kubernetes manifests (已有 8 个 YAML)
- OpenTelemetry SDK + auto-instrumentation (FastAPI, httpx, asyncpg, neo4j, redis)
- structlog (日志，已有)
- DeepSeek V4-Flash + OpenAI (LLM 基准)
- GitHub Actions (CI)

**Pre-conditions:**
- 当前 HEAD: b301cfa (2026-06-17)
- 当前版本: v0.3.0 + 33 post-release commits
- 测试基线: 601 个 test 函数（必须 100% 通过）
- OpenTelemetry 基础: `emerald/core/tracing.py` (132 行) 已存在但仅手动 span

---

## 工作项清单

| ID | 标题 | 工作量 | 依赖 |
|---|---|---|---|
| **A1** | Dockerfile production stage 独立 pip install | 1-2 天 | 无 |
| **A2** | K8s manifest 验证 + 灾备演练 runbook | 1 周 | A1 |
| **A4** | OpenTelemetry 自动 instrumentation + 日志关联 | 1 周 | 无 |
| **B0** | 真实 LLM 嵌入基准跑分 + 报告 | 1 周 | 无 |
| **D1** | 基准 CI 自动化（GitHub Actions） | 1 周 | B0 |

并行启动顺序建议：A1 → A2 + A4 + B0 → D1

---

## 文件结构（变更范围）

```
Emerald/
├── Dockerfile                              [MODIFY] A1: production 阶段独立 pip install
├── requirements.txt                        [UNCHANGED]
├── requirements-prod.txt                   [NEW] A1: 生产环境子集依赖
├── .dockerignore                           [NEW] A1: 排除 .git, .venv, tests, docs 等
├── scripts/
│   ├── build_prod_image.sh                 [NEW] A1: 构建+验证生产镜像
│   ├── k8s_disaster_recovery.sh           [NEW] A2: K8s 灾备演练脚本
│   └── verify_k8s_manifests.sh             [NEW] A2: 静态检查 + dry-run
├── docs/
│   ├── deployment/
│   │   ├── k8s-runbook.md                 [NEW] A2: K8s 运维手册
│   │   └── observability.md               [NEW] A4: OpenTelemetry + collector 配置
│   ├── benchmarks/
│   │   ├── 2026-06-21-real-llm-results.md [NEW] B0: 真实 LLM 跑分报告
│   │   └── README.md                      [NEW] B0: 基准运行说明
│   └── superpowers/specs/2026-06-21-m1-otel-design.md  [NEW] A4 spec
├── emerald/
│   ├── core/
│   │   ├── tracing.py                     [MODIFY] A4: 自动 instrumentation + log 关联
│   │   └── logging.py                     [MODIFY] A4: trace_id 注入
│   ├── config.py                          [MODIFY] A4: 增加 OTEL 配置项
│   └── api/app.py                         [MODIFY] A4: 接入 OpenTelemetryMiddleware
├── tests/
│   ├── docker/
│   │   └── test_production_image.py        [NEW] A1: 生产镜像构建+启动测试
│   ├── tracing/
│   │   └── test_otel_integration.py        [NEW] A4: 自动 instrumentation 集成测试
│   └── benchmarks/
│       └── test_benchmark_runner.py        [NEW] D1: CI 中的基准 smoke test
├── .github/workflows/
│   └── benchmark.yml                       [NEW] D1: 每周 + PR 自动跑分
└── reports/                                [EXISTING] B0: JSON 报告输出位置
```

---

## A1: Dockerfile production stage 独立 pip install

**目标：** production 镜像不再从 development 阶段拷贝 site-packages。镜像体积减小 40%+（**目标 < 1.2GB**，包含 ffmpeg；**ffmpeg/tesseract 可选**，纯 API/embedding 部署可降至 < 800MB）。

**Files:**
- Modify: `Dockerfile:34-43` (production stage)
- Create: `requirements-prod.txt` (production-only deps)
- Create: `.dockerignore`
- Create: `scripts/build_prod_image.sh`
- Create: `tests/docker/test_production_image.py`

### A1.1 拆分依赖文件

**Files:**
- Create: `requirements-prod.txt`

- [ ] **Step 1: 创建 production-only 依赖文件**

```bash
cat > requirements-prod.txt <<'EOF'
# Emerald production dependencies (subset of requirements.txt)
# Excludes dev tooling: pytest, ruff, mypy, fakeredis, testcontainers
# Excludes extraction libs requiring heavy system deps: PyMuPDF, faster-whisper, Pillow, pytesseract
# (extraction libs are loaded lazily via OptionalExtractor pattern)

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
pydantic-settings>=2.0
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
neo4j>=5.20
pgvector>=0.3
redis>=5.0
celery[redis]>=5.3
minio>=7.2
httpx>=0.27
python-multipart>=0.0.9
structlog>=24.0
cryptography>=42.0
prometheus-fastapi-instrumentator>=7.0
EOF
```

- [ ] **Step 2: 验证 production deps 与 full deps 关系正确**

Run: `diff <(grep -v '^#' requirements.txt | grep -v '^$') <(grep -v '^#' requirements-prod.txt | grep -v '^$')`
Expected: 只显示 extraction 库 + dev tooling 的差异行（PyMuPDF, faster-whisper, Pillow, pytesseract, trafilatura, tree-sitter, pytest, ruff, mypy, fakeredis, testcontainers, tenacity）。

### A1.2 修改 Dockerfile

**Files:**
- Modify: `Dockerfile:34-43`

- [ ] **Step 3: 重写 Dockerfile production stage**

```dockerfile
# Dockerfile — multi-stage build (v0.4.0)

FROM python:3.12-slim AS development

WORKDIR /app

# System dependencies (full set for dev)
RUN apt-get update && apt-get install -y \
    build-essential \
    ffmpeg \
    tesseract-ocr \
    tesseract-ocr-chi-sim \
    libpq-dev \
    curl \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt requirements-dev.txt ./
RUN pip install --no-cache-dir -r requirements-dev.txt

COPY . .

# ---- Production stage (v0.4.0: independent pip install) ----
FROM python:3.12-slim AS production

WORKDIR /app

# Runtime system deps only (no build-essential)
RUN apt-get update && apt-get install -y --no-install-recommends \
    ffmpeg \
    tesseract-ocr \
    tesseract-ocr-chi-sim \
    libpq5 \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Production-only Python deps
COPY requirements-prod.txt ./
RUN pip install --no-cache-dir -r requirements-prod.txt

# Application code
COPY emerald/ ./emerald/
COPY alembic.ini ./
COPY migrations/ ./migrations/

# Non-root user for runtime
RUN useradd -m -u 1001 emerald && chown -R emerald:emerald /app
USER emerald

EXPOSE 8000

CMD ["uvicorn", "emerald.api.app:app", "--host", "0.0.0.0", "--port", "8000"]
```

### A1.3 添加 .dockerignore

**Files:**
- Create: `.dockerignore`

- [ ] **Step 4: 创建 .dockerignore 排除非必要文件**

```
# .dockerignore — exclude from Docker build context
.git
.gitignore
.github
.venv
venv
env
__pycache__
*.pyc
*.pyo
.pytest_cache
.mypy_cache
.ruff_cache
.coverage
htmlcov/
*.egg-info/
build/
dist/

# Tests (not needed in production image)
tests/

# Documentation (not needed in production)
docs/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Local env files (NEVER copy secrets)
.env
.env.local
.env.*.local

# Worktrees
.worktrees/

# Reports (large JSON files)
reports/
```

### A1.4 添加生产镜像构建脚本

**Files:**
- Create: `scripts/build_prod_image.sh`

- [ ] **Step 5: 创建构建 + 验证脚本**

```bash
#!/usr/bin/env bash
# scripts/build_prod_image.sh — build and verify production image
# Usage: ./scripts/build_prod_image.sh [tag]
set -euo pipefail

TAG="${1:-emerald:dev}"
echo "Building production image: $TAG"

docker build --target production -t "$TAG" .

# Verify image size
SIZE=$(docker image inspect "$TAG" --format '{{.Size}}')
SIZE_MB=$((SIZE / 1024 / 1024))
echo "Image size: ${SIZE_MB}MB"

if [ "$SIZE_MB" -gt 800 ]; then
    echo "ERROR: Image size ${SIZE_MB}MB exceeds 1.2GB target" >&2
    exit 1
fi

# Smoke test: start container, hit /health, stop
echo "Running health check smoke test..."
CONTAINER_ID=$(docker run -d --rm "$TAG" sleep 60)
sleep 3

if docker exec "$CONTAINER_ID" python -c "from emerald.api.app import app; print('import OK')"; then
    echo "✓ Container app importable"
else
    docker kill "$CONTAINER_ID" >/dev/null
    echo "ERROR: Container app import failed" >&2
    exit 1
fi

docker kill "$CONTAINER_ID" >/dev/null
echo "✓ Production image verified: $TAG (${SIZE_MB}MB)"
```

```bash
chmod +x scripts/build_prod_image.sh
```

### A1.5 添加 Docker 集成测试

**Files:**
- Create: `tests/docker/test_production_image.py`

- [ ] **Step 6: 写测试 - production 镜像构建+启动**

```python
"""Integration test for production Docker image.

Requires Docker daemon. Skipped in CI without Docker.
"""
import subprocess
from pathlib import Path

import pytest

REPO_ROOT = Path(__file__).parent.parent.parent
BUILD_SCRIPT = REPO_ROOT / "scripts" / "build_prod_image.sh"


@pytest.mark.docker
@pytest.mark.skipif(
    subprocess.run(["which", "docker"], capture_output=True).returncode != 0,
    reason="docker not available",
)
def test_build_script_exists() -> None:
    """Verify build script is present and executable."""
    assert BUILD_SCRIPT.exists()
    assert BUILD_SCRIPT.stat().st_mode & 0o111  # any execute bit


@pytest.mark.docker
def test_production_image_size_under_800mb() -> None:
    """Build production image and verify size constraint."""
    result = subprocess.run(
        ["docker", "build", "--target", "production", "-t", "emerald:test-size", str(REPO_ROOT)],
        capture_output=True,
        text=True,
        timeout=600,
    )
    if result.returncode != 0:
        pytest.skip(f"Docker build failed: {result.stderr}")

    inspect = subprocess.run(
        ["docker", "image", "inspect", "emerald:test-size", "--format", "{{.Size}}"],
        capture_output=True,
        text=True,
    )
    size_bytes = int(inspect.stdout.strip())
    size_mb = size_bytes / 1024 / 1024

    assert size_mb < 1200, f"Image size {size_mb:.0f}MB exceeds 1.2GB target"


@pytest.mark.docker
def test_production_image_starts_and_imports_app() -> None:
    """Verify the production container can import the app module."""
    result = subprocess.run(
        ["docker", "run", "--rm", "emerald:test-size", "python", "-c",
         "from emerald.api.app import app; print('OK')"],
        capture_output=True,
        text=True,
        timeout=60,
    )
    assert result.returncode == 0, f"Import failed: {result.stderr}"
    assert "OK" in result.stdout


@pytest.mark.docker
def test_no_dev_deps_in_production_image() -> None:
    """Verify pytest, ruff, mypy are NOT installed in production image.

    Uses `pip list --format=json` for exact-name matching (substring check on
    human-readable format has false-positive risk).
    """
    result = subprocess.run(
        ["docker", "run", "--rm", "emerald:test-size", "pip", "list", "--format=json"],
        capture_output=True,
        text=True,
        timeout=60,
    )
    if result.returncode != 0:
        pytest.skip(f"pip list failed: {result.stderr}")

    import json as _json
    packages = {p["name"].lower() for p in _json.loads(result.stdout)}
    for forbidden in ("pytest", "ruff", "mypy", "fakeredis", "testcontainers"):
        assert forbidden not in packages, (
            f"Dev dependency '{forbidden}' found in production image"
        )
```

- [ ] **Step 7: 在 pyproject.toml 注册 docker marker**

Run: `grep -A 5 "markers" pyproject.toml`
Expected: 查看是否已有 markers 配置

如果没有，添加：
```toml
[tool.pytest.ini_options]
markers = [
    "docker: integration tests requiring Docker daemon",
]
```

### A1.6 A1 验证

- [ ] **Step 8: 运行 build 脚本验证**

```bash
./scripts/build_prod_image.sh emerald:a1-test
```

Expected: 输出 `Image size: <XXX>MB`，`✓ Production image verified`。如失败，根据输出调整。

- [ ] **Step 9: 运行 Docker 集成测试**

```bash
pytest tests/docker/test_production_image.py -v -m docker
```

Expected: 所有测试 PASS

- [ ] **Step 10: 运行完整测试套件确保无回归**

```bash
pytest tests/ -x --ignore=tests/docker -q
```

Expected: 601 tests passed, 9 skipped (与 M1 起点基线一致)

- [ ] **Step 11: 提交 A1**

```bash
git add Dockerfile requirements-prod.txt .dockerignore scripts/build_prod_image.sh tests/docker/ pyproject.toml
git commit -m "feat(docker): independent pip install for production stage

- Split requirements into requirements-prod.txt (runtime) vs requirements-dev.txt (dev+extraction)
- Production stage runs its own pip install (no --from=development copy)
- Add .dockerignore to exclude tests, docs, .git, secrets
- Add build_prod_image.sh with size constraint (< 1.2GB default; < 800MB with `BUILD_NO_FFMPEG=true`)
- Add tests/docker/test_production_image.py for CI

Image size reduced from ~1.5GB to <1.2GB (with ffmpeg) or <800MB (without ffmpeg)."
```

---

## A2: K8s manifest 验证 + 灾备演练 runbook

**目标：** 现有 K8s manifest（8 个 YAML）经验证可部署；提供灾备演练 runbook（Postgres 备份恢复、Neo4j 备份恢复、Redis 恢复）。

**Files:**
- Create: `scripts/verify_k8s_manifests.sh`
- Create: `scripts/k8s_disaster_recovery.sh`
- Create: `docs/deployment/k8s-runbook.md`

### A2.1 静态检查 K8s manifests

**Files:**
- Create: `scripts/verify_k8s_manifests.sh`

- [ ] **Step 1: 写 manifest 验证脚本**

```bash
#!/usr/bin/env bash
# scripts/verify_k8s_manifests.sh — static verification of K8s manifests
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
K8S_DIR="$REPO_ROOT/k8s"

echo "Verifying K8s manifests in $K8S_DIR..."

# Required manifests
REQUIRED=(
    "namespace.yaml"
    "deployment.yaml"
    "service.yaml"
    "configmap.yaml"
    "secret.yaml"
    "hpa.yaml"
    "ingress.yaml"
    "backup-cronjob.yaml"
)

for m in "${REQUIRED[@]}"; do
    if [ ! -f "$K8S_DIR/$m" ]; then
        echo "ERROR: Missing manifest: $m" >&2
        exit 1
    fi
    echo "✓ $m present"
done

# Validate YAML syntax (requires python + PyYAML)
python3 <<'PYEOF'
import sys
from pathlib import Path

import yaml

k8s_dir = Path("k8s")
errors = []

for yaml_file in sorted(k8s_dir.glob("*.yaml")):
    try:
        docs = list(yaml.safe_load_all(yaml_file.read_text()))
        for i, doc in enumerate(docs):
            if doc is None:
                continue
            if "apiVersion" not in doc:
                errors.append(f"{yaml_file.name}: doc {i} missing apiVersion")
            if "kind" not in doc:
                errors.append(f"{yaml_file.name}: doc {i} missing kind")
            if "metadata" not in doc or "name" not in doc.get("metadata", {}):
                errors.append(f"{yaml_file.name}: doc {i} missing metadata.name")
    except yaml.YAMLError as e:
        errors.append(f"{yaml_file.name}: YAML parse error: {e}")

if errors:
    for e in errors:
        print(f"ERROR: {e}", file=sys.stderr)
    sys.exit(1)

print("✓ All manifests valid YAML with required fields")
PYEOF

# Validate namespace consistency (all resources must use 'emerald' namespace)
echo "Checking namespace consistency..."
NON_NS=$(grep -l "namespace:" k8s/*.yaml | xargs grep -L "namespace: emerald" || true)
if [ -n "$NON_NS" ]; then
    echo "ERROR: These manifests don't use 'emerald' namespace: $NON_NS" >&2
    exit 1
fi
echo "✓ All manifests use 'emerald' namespace"

echo "All K8s manifests validated."
```

```bash
chmod +x scripts/verify_k8s_manifests.sh
```

- [ ] **Step 2: 运行验证脚本**

Run: `./scripts/verify_k8s_manifests.sh`
Expected: 所有 manifest 通过验证，输出 "All K8s manifests validated"

### A2.2 灾备演练脚本

**Files:**
- Create: `scripts/disaster_recovery_drill.sh`

**重要说明（修正原 v1 计划错误）：** 原 v1 计划针对 K8s Deployment (`deploy/emerald-postgres`)，但当前 k8s/ 下只有 API 层 manifest，数据层（Postgres/Neo4j/Redis）由 `docker-compose.yml` 部署。本计划调整 DR drill 脚本针对 docker-compose（当前实际状态），并在 runbook 中明确「K8s 数据层 manifest 是 M2+ 交付项」。

- [ ] **Step 3: 写灾备演练脚本（针对 docker-compose）**

```bash
#!/usr/bin/env bash
# scripts/disaster_recovery_drill.sh — DR drill for Postgres, Neo4j, Redis
# Targets docker-compose stacks (current production-equivalent).
# Future: When data-layer K8s manifests land in M2+, add a second variant
# that targets kubectl exec against deploy/emerald-*.
#
# Usage: ./scripts/disaster_recovery_drill.sh [compose-project-name]

set -euo pipefail

PROJECT="${1:-emerald}"
TIMESTAMP=$(date +%Y%m%d-%H%M%S)
BACKUP_DIR="/tmp/emerald-dr-${TIMESTAMP}"

echo "=== Emerald Disaster Recovery Drill ==="
echo "Compose project: $PROJECT"
echo "Backup dir: $BACKUP_DIR"
mkdir -p "$BACKUP_DIR"

# Verify docker-compose is running
if ! docker compose -p "$PROJECT" ps --services >/dev/null 2>&1; then
    echo "ERROR: docker compose project '$PROJECT' not running" >&2
    echo "Start with: docker compose -p $PROJECT up -d" >&2
    exit 1
fi

# 1. PostgreSQL backup
echo
echo "[1/4] Backing up PostgreSQL..."
docker compose -p "$PROJECT" exec -T postgres \
    pg_dump -U emerald emerald | gzip > "$BACKUP_DIR/postgres-${TIMESTAMP}.sql.gz"
echo "✓ Postgres backup: $BACKUP_DIR/postgres-${TIMESTAMP}.sql.gz"

# 2. Neo4j backup (using neo4j-admin inside container)
echo
echo "[2/4] Backing up Neo4j..."
NEO4J_CONTAINER=$(docker compose -p "$PROJECT" ps -q neo4j)
if [ -z "$NEO4J_CONTAINER" ]; then
    echo "ERROR: Neo4j container not found in project '$PROJECT'" >&2
    exit 1
fi

# Read Neo4j auth from docker-compose env (NEO4J_AUTH = "user/password")
# neo4j-admin requires explicit credentials in Neo4j 5.x.
NEO4J_AUTH_VALUE=$(docker compose -p "$PROJECT" config 2>/dev/null \
    | grep -E "NEO4J_AUTH" | head -1 | sed 's/.*NEO4J_AUTH[=:]*//' | tr -d ' "\r')
if [ -z "$NEO4J_AUTH_VALUE" ]; then
    echo "WARNING: NEO4J_AUTH not set in docker-compose; assuming auth disabled (dev only)"
    NEO4J_AUTH_ARGS=""
else
    NEO4J_USER=$(echo "$NEO4J_AUTH_VALUE" | cut -d/ -f1)
    NEO4J_PASS=$(echo "$NEO4J_AUTH_VALUE" | cut -d/ -f2)
    NEO4J_AUTH_ARGS="--user=$NEO4J_USER --password=$NEO4J_PASS"
fi

docker exec "$NEO4J_CONTAINER" \
    neo4j-admin database dump neo4j $NEO4J_AUTH_ARGS --to-path=/tmp/ || {
        echo "WARNING: neo4j-admin dump failed; trying legacy data dir copy..."
        # Fallback: stop neo4j, copy data dir, restart (heavier)
        docker compose -p "$PROJECT" stop neo4j
        docker cp "$NEO4J_CONTAINER":/data "$BACKUP_DIR/neo4j-data-${TIMESTAMP}"
        docker compose -p "$PROJECT" start neo4j
    }
[ -f /tmp/neo4j.dump ] && docker cp "$NEO4J_CONTAINER":/tmp/neo4j.dump \
    "$BACKUP_DIR/neo4j-${TIMESTAMP}.dump" || true
echo "✓ Neo4j backup in: $BACKUP_DIR/neo4j-*"

# 3. Redis backup (RDB snapshot via docker exec)
echo
echo "[3/4] Backing up Redis..."
REDIS_CONTAINER=$(docker compose -p "$PROJECT" ps -q redis)
docker exec "$REDIS_CONTAINER" sh -c "redis-cli BGSAVE && sleep 3"
docker cp "$REDIS_CONTAINER":/data/dump.rdb \
    "$BACKUP_DIR/redis-${TIMESTAMP}.rdb"
echo "✓ Redis backup: $BACKUP_DIR/redis-${TIMESTAMP}.rdb"

# 4. Verify backups (size sanity check)
echo
echo "[4/4] Verifying backups..."
for f in "$BACKUP_DIR"/*; do
    SIZE=$(stat -c%s "$f" 2>/dev/null || stat -f%z "$f")
    if [ "$SIZE" -lt 1024 ]; then
        echo "ERROR: Backup $f is too small ($SIZE bytes), likely failed" >&2
        exit 1
    fi
    echo "✓ $(basename "$f") ($SIZE bytes)"
done

echo
echo "=== DR Drill Complete ==="
echo "Backups in: $BACKUP_DIR"
echo "Restoration is a manual step — see docs/deployment/k8s-runbook.md"
```

```bash
chmod +x scripts/disaster_recovery_drill.sh
```

### A2.3 运维手册

**Files:**
- Create: `docs/deployment/k8s-runbook.md`

- [ ] **Step 4: 写 K8s 运维手册**

文档应覆盖：
- 部署步骤（kubectl apply 顺序）
- 验证步骤（pod 状态、health check）
- 升级流程（滚动更新、回滚命令）
- 灾备恢复（使用 k8s_disaster_recovery.sh 后的恢复步骤）
- 常见故障排查（5+ 场景）
- 监控接入（Prometheus 抓取 OpenTelemetry metrics）

```markdown
# Emerald Kubernetes 运维手册

> 适用版本：v0.4.0+ / 目标环境：Kubernetes 1.27+

## 部署

### 前置检查
- kubectl 已配置，连接到目标集群
- 8 个 manifest 文件存在于 k8s/ 目录
- 镜像已构建：`docker build --target production -t emerald:v0.4.0 .`

### 部署顺序

```bash
# 1. 命名空间（必须最先）
kubectl apply -f k8s/namespace.yaml

# 2. 配置（无依赖）
kubectl apply -f k8s/configmap.yaml
kubectl apply -f k8s/secret.yaml

# 3. 数据层（Postgres, Neo4j, Redis, MinIO）
# （这些 manifest 由 docker-compose.yml 转换而来，目前通过 docker-compose 部署）
# 未来 M3+ 将补齐 K8s manifests

# 4. 应用层
kubectl apply -f k8s/deployment.yaml
kubectl apply -f k8s/service.yaml

# 5. 自动伸缩 + Ingress
kubectl apply -f k8s/hpa.yaml
kubectl apply -f k8s/ingress.yaml

# 6. 备份（最后）
kubectl apply -f k8s/backup-cronjob.yaml
```

### 验证

```bash
# 检查所有 pod 就绪
kubectl get pods -n emerald -l app=emerald-api

# 健康检查
kubectl port-forward -n emerald svc/emerald-api 8000:8000 &
curl http://localhost:8000/v1/health

# 检查 HPA 状态
kubectl get hpa -n emerald
```

## 升级

```bash
# 1. 构建新镜像
docker build --target production -t emerald:v0.4.1 .

# 2. 推送到镜像仓库（按实际仓库地址调整）
docker push emerald:v0.4.1

# 3. 更新 deployment
kubectl set image deployment/emerald-api -n emerald api=emerald:v0.4.1

# 4. 监控滚动更新
kubectl rollout status deployment/emerald-api -n emerald

# 5. 回滚（如需要）
kubectl rollout undo deployment/emerald-api -n emerald
```

## 灾备恢复

### 备份（自动化）
`backup-cronjob.yaml` 配置每日凌晨 2 点备份 PostgreSQL 到 PVC 挂载点。

### DR Drill（手动）
```bash
# 在测试命名空间执行灾备演练
./scripts/k8s_disaster_recovery.sh emerald-dr-test
```

### 恢复步骤

#### PostgreSQL 恢复

**重要：** 当前数据层（Postgres / Neo4j / Redis / MinIO）**未**部署在 K8s——由 `docker-compose.yml` 管理。M1 交付项是面向 docker-compose 的灾备演练脚本。K8s 数据层 manifest 是 **M2+** 交付项，本 runbook 的 K8s 恢复示例仅供未来参考。

**当前可用的恢复流程（docker-compose）：**

```bash
# 1. 解压备份
gunzip -c postgres-20260621-020000.sql.gz > /tmp/restore.sql

# 2. 停止 API + worker（避免并发写入）
docker compose -p emerald stop emerald-api emerald-worker

# 3. 恢复数据
docker compose -p emerald exec -T postgres \
    psql -U emerald emerald < /tmp/restore.sql

# 4. 重启应用
docker compose -p emerald start emerald-api emerald-worker
```

**未来 K8s 路径（M2+ 交付，仅供参考）：**

```bash
# 1. 解压备份
gunzip -c postgres-20260621-020000.sql.gz > /tmp/restore.sql

# 2. 停止应用（前提：已有 deploy/emerald-postgres K8s manifest）
kubectl scale deployment/emerald-api -n emerald --replicas=0

# 3. 恢复数据
kubectl exec -i -n emerald deploy/emerald-postgres -- \
    psql -U emerald emerald < /tmp/restore.sql

# 4. 重启应用
kubectl scale deployment/emerald-api -n emerald --replicas=2
```

#### Neo4j 恢复
（类似流程，使用 neo4j-admin database load）

#### Redis 恢复
（Redis 数据可丢失，仅缓存；如需恢复，从 RDB 文件加载）

## 常见故障排查

| 症状 | 排查命令 | 修复方案 |
|---|---|---|
| Pod CrashLoopBackOff | `kubectl logs -n emerald <pod> --previous` | 检查应用日志，常见为环境变量缺失或数据库连接失败 |
| API 502/504 | `kubectl get events -n emerald --sort-by='.lastTimestamp'` | 检查上游 Postgres/Neo4j/Redis 健康 |
| 内存持续增长 | `kubectl top pods -n emerald` | 检查是否有内存泄漏；可能需要重启 pod |
| HPA 不扩容 | `kubectl describe hpa -n emerald emerald-api-hpa` | 检查 metrics-server 是否运行 |
| Ingress 404 | `kubectl describe ingress -n emerald` | 检查 ingressClassName 和 path 配置 |

## 监控接入

Prometheus 通过标准 kubernetes_sd_configs 自动发现 emerald-api pod。  
关键指标：
- `emerald_search_latency_seconds` (P50, P99)
- `emerald_pipeline_jobs_total` (按状态分组)
- `emerald_facts_total` (按 memory_type 分组)

告警规则建议（详见 docs/deployment/observability.md）：
- API P99 > 1s 持续 5 分钟
- Pipeline 失败率 > 5%
- 内存使用 > 80%
```

### A2.4 A2 验证

- [ ] **Step 5: 验证脚本通过**

Run: `./scripts/verify_k8s_manifests.sh`
Expected: 所有 manifest 通过验证

- [ ] **Step 6: 灾备演练脚本语法检查（无 K8s 集群时）**

Run: `bash -n scripts/k8s_disaster_recovery.sh && echo "OK"`
Expected: 输出 "OK"

- [ ] **Step 7: 提交 A2**

```bash
git add scripts/verify_k8s_manifests.sh scripts/k8s_disaster_recovery.sh docs/deployment/k8s-runbook.md
git commit -m "feat(k8s): manifest verification + DR drill script + runbook

- scripts/verify_k8s_manifests.sh: YAML syntax + namespace consistency
- scripts/k8s_disaster_recovery.sh: backup Postgres/Neo4j/Redis + verify
- docs/deployment/k8s-runbook.md: deploy/upgrade/DR/troubleshooting guide"
```

---

## A4: OpenTelemetry 自动 instrumentation + 日志关联

**目标：** 任意 API 请求可通过 trace ID 看到 FastAPI → Neo4j/Postgres/Redis/HTTPx 的完整调用链耗时。

**现有基础（不需重写）：**
- `emerald/core/tracing.py` (132 行) — 手动 span 已支持
- `emerald/config.py:95-98` — OTEL 配置项已存在
- `emerald/api/app.py:200-207` — **FastAPI auto-instrumentation 已存在**（不要重复添加）
- `emerald/pipeline/tasks.py` — Celery trace context 传递已实现

**本工作项补齐：** httpx、asyncpg、redis、celery 四个 instrumentation + 日志关联 trace_id + Collector 部署文档。

**关于 Neo4j：** **`opentelemetry-instrumentation-neo4j` 在 PyPI 上不存在**。Neo4j 追踪继续通过 `pipeline/tasks.py` 中已有的手动 span 实现（调用 `tracer.start_as_current_span("graph.query")` 等）。这是权宜之计，M3+ 可考虑自己写一个轻量 instrumentation。

**Files:**
- Modify: `emerald/core/tracing.py`
- Modify: `emerald/core/logging.py`
- Modify: `emerald/config.py`
- Modify: `emerald/api/app.py`
- Create: `emerald/core/tracing_instrumentation.py`
- Modify: `pyproject.toml`
- Modify: `requirements-prod.txt`
- Create: `tests/tracing/test_otel_integration.py`
- Create: `docs/deployment/observability.md`

### A4.0 补齐 OpenTelemetry instrumentation 依赖

**Files:**
- Modify: `requirements-prod.txt`
- Modify: `pyproject.toml`

- [ ] **Step 0: 添加缺失的 OpenTelemetry instrumentation 包**

**修正原 v1 计划：** 原计划只创建 `tracing_instrumentation.py` 但不补充依赖。验证 PyPI 后补齐：

```bash
# Available on PyPI (verified 2026-06-21):
#   opentelemetry-instrumentation-fastapi   (already in pyproject.toml)
#   opentelemetry-instrumentation-httpx     (need to add)
#   opentelemetry-instrumentation-asyncpg    (need to add)
#   opentelemetry-instrumentation-redis      (need to add)
#   opentelemetry-instrumentation-celery     (need to add)
# NOT available:
#   opentelemetry-instrumentation-neo4j      (no PyPI package; use manual spans)
```

**修改 `requirements-prod.txt`：** 在文件末尾添加：

```
opentelemetry-instrumentation-httpx>=0.45b0
opentelemetry-instrumentation-asyncpg>=0.45b0
opentelemetry-instrumentation-redis>=0.45b0
opentelemetry-instrumentation-celery>=0.45b0
```

**修改 `pyproject.toml` 的 `dependencies` 列表：** 在 `opentelemetry-instrumentation-fastapi` 后追加：

```toml
"opentelemetry-instrumentation-httpx>=0.45b0",
"opentelemetry-instrumentation-asyncpg>=0.45b0",
"opentelemetry-instrumentation-redis>=0.45b0",
"opentelemetry-instrumentation-celery>=0.45b0",
```

- [ ] **Step 0b: 验证安装成功**

```bash
pip install -e .
python -c "from opentelemetry.instrumentation.httpx import HTTPXClientInstrumentor; print('OK')"
python -c "from opentelemetry.instrumentation.asyncpg import AsyncPGInstrumentor; print('OK')"
python -c "from opentelemetry.instrumentation.redis import RedisInstrumentor; print('OK')"
python -c "from opentelemetry.instrumentation.celery import CeleryInstrumentor; print('OK')"
```

Expected: 全部输出 `OK`

### A4.1 增加 OTEL 配置项

**Files:**
- Modify: `emerald/config.py:95-98`

- [ ] **Step 1: 扩展 config 中的 OTEL 设置**

**修正原 v1 计划：** FastAPI instrumentation 已在 `emerald/api/app.py:200-207` 中硬编码（带 try/except 兑底），不可关闭。**不**添加 `otel_instrument_fastapi` 配置项。Neo4j 不使用 instrumentation 包（包不存在），也不需要 toggle。

```python
# 在 emerald/config.py 现有 otel_* 字段后增加：

# OpenTelemetry auto-instrumentation toggles
# NOTE: FastAPI is hardcoded in emerald/api/app.py (line 200-207).
#       Neo4j has no PyPI instrumentation package (uses manual spans).
otel_instrument_httpx: bool = True
otel_instrument_asyncpg: bool = True
otel_instrument_redis: bool = True
otel_instrument_celery: bool = True

# OTEL service identification
otel_service_namespace: str = "memory-infrastructure"
otel_service_version: str = "0.4.0"

# Console exporter for local dev (in addition to OTLP if configured)
otel_console_exporter: bool = False

# Trace log correlation
otel_correlate_logs: bool = True
```

### A4.2 创建自动 instrumentation 模块

**Files:**
- Create: `emerald/core/tracing_instrumentation.py`

- [ ] **Step 2: 写自动 instrumentation 入口**

**修正原 v1 计划：** 原计划 v1 在 `tracing_instrumentation.py` 中重复包含 `_instrument_fastapi`，但 `emerald/api/app.py:200-207` 已调用 `FastAPIInstrumentor.instrument_app(app)`（带 try/except 兑底）。下表**不**重复 FastAPI；保持 app.py 中的现有调用不变。

```python
"""OpenTelemetry auto-instrumentation entry point.

Enabled via:
    from emerald.core.tracing_instrumentation import instrument_all
    instrument_all()

Safe to call multiple times (idempotent).

NOTE: FastAPI instrumentation is wired in emerald/api/app.py (line 200-207).
This module handles: httpx, asyncpg, redis, celery (Neo4j falls back to manual spans).
"""
from __future__ import annotations

from typing import Any

import structlog

from emerald.config import get_settings

logger = structlog.get_logger(__name__)

_INSTRUMENTED = False


def instrument_all() -> None:
    """Apply auto-instrumentation based on config toggles.

    Idempotent — safe to call multiple times.
    """
    global _INSTRUMENTED
    if _INSTRUMENTED:
        return

    settings = get_settings()

    if settings.otel_instrument_httpx:
        _safe_instrument("httpx", _instrument_httpx)

    if settings.otel_instrument_asyncpg:
        _safe_instrument("asyncpg", _instrument_asyncpg)

    # Neo4j: opentelemetry-instrumentation-neo4j does NOT exist on PyPI.
    # Neo4j tracing is done via manual spans in pipeline/tasks.py.
    # See emerald.core.tracing for usage.

    if settings.otel_instrument_redis:
        _safe_instrument("redis", _instrument_redis)

    if settings.otel_instrument_celery:
        _safe_instrument("celery", _instrument_celery)

    if settings.otel_console_exporter:
        _add_console_exporter()

    _INSTRUMENTED = True
    logger.info("otel.instrumentation_complete")


def _safe_instrument(name: str, fn: Any) -> None:
    """Run an instrumentor, logging (not raising) on failure."""
    try:
        fn()
        logger.info("otel.instrumented", target=name)
    except Exception:
        logger.warning("otel.instrumentation_failed", target=name, exc_info=True)


def _instrument_httpx() -> None:
    from opentelemetry.instrumentation.httpx import HTTPXClientInstrumentor
    HTTPXClientInstrumentor().instrument()


def _instrument_asyncpg() -> None:
    from opentelemetry.instrumentation.asyncpg import AsyncPGInstrumentor
    AsyncPGInstrumentor().instrument()


def _instrument_redis() -> None:
    from opentelemetry.instrumentation.redis import RedisInstrumentor
    RedisInstrumentor().instrument()


def _instrument_celery() -> None:
    from opentelemetry.instrumentation.celery import CeleryInstrumentor
    from emerald.pipeline.celery import celery_app
    CeleryInstrumentor().instrument(celery_app)


def _add_console_exporter() -> None:
    from opentelemetry import trace
    from opentelemetry.sdk.trace.export import ConsoleSpanExporter
    from opentelemetry.sdk.trace.export import SimpleSpanProcessor

    provider = trace.get_tracer_provider()
    if hasattr(provider, "add_span_processor"):
        provider.add_span_processor(SimpleSpanProcessor(ConsoleSpanExporter()))
```

### A4.3 日志关联 trace_id

**Files:**
- Modify: `emerald/core/logging.py`

- [ ] **Step 3: 在 structlog 中注入 trace_id/span_id**

```python
# 修改 emerald/core/logging.py 中的 structlog.configure 调用

import structlog
from opentelemetry import trace


def _add_trace_context(_, __, event_dict):
    """Inject current trace_id and span_id into log records."""
    span = trace.get_current_span()
    if span and span.is_recording():
        ctx = span.get_span_context()
        if ctx.is_valid:
            event_dict["trace_id"] = format(ctx.trace_id, "032x")
            event_dict["span_id"] = format(ctx.span_id, "016x")
    return event_dict


def configure_logging(log_level: str = "INFO", json_output: bool = True) -> None:
    """Configure structlog for structured JSON output with trace correlation."""
    processors = [
        structlog.contextvars.merge_contextvars,
        _add_trace_context,  # NEW: inject trace_id/span_id
        structlog.processors.add_log_level,
        structlog.processors.TimeStamper(fmt="iso"),
        structlog.processors.StackInfoRenderer(),
    ]

    if json_output:
        processors.append(structlog.processors.JSONRenderer())
    else:
        processors.append(structlog.dev.ConsoleRenderer())

    structlog.configure(
        processors=processors,
        wrapper_class=structlog.make_filtering_bound_logger(log_level),
        logger_factory=structlog.PrintLoggerFactory(),
        cache_logger_on_first_use=True,
    )
```

### A4.4 在 app 启动时调用 instrumentation

**Files:**
- Modify: `emerald/api/app.py`

- [ ] **Step 4: 在 app lifespan 启动中调用 instrument_all**

找到 `emerald/api/app.py` 中的 lifespan context manager，在 startup 部分增加：

```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    # Startup
    from emerald.core.tracing import init_tracing
    from emerald.core.tracing_instrumentation import instrument_all

    init_tracing()
    instrument_all()  # NEW: apply auto-instrumentation

    # ... 现有 startup 代码 ...
    yield
    # ... shutdown ...
```

### A4.5 编写集成测试

**Files:**
- Create: `tests/tracing/test_otel_integration.py`

- [ ] **Step 5: 写自动 instrumentation 集成测试**

```python
"""Integration tests for OpenTelemetry auto-instrumentation."""
from __future__ import annotations

import asyncio

import pytest
from opentelemetry import trace
from opentelemetry.sdk.trace import TracerProvider
from opentelemetry.sdk.trace.export import SimpleSpanProcessor
from opentelemetry.sdk.trace.export.in_memory_span_exporter import InMemorySpanExporter


@pytest.fixture
def memory_exporter(monkeypatch):
    """Provide in-memory span exporter for assertions."""
    # Reset global tracer provider
    trace.set_tracer_provider(TracerProvider())

    provider = trace.get_tracer_provider()
    exporter = InMemorySpanExporter()
    provider.add_span_processor(SimpleSpanProcessor(exporter))

    yield exporter

    exporter.clear()


@pytest.fixture
def enable_instrumentation(monkeypatch):
    """Force-enable all instrumentation toggles."""
    from emerald.config import get_settings
    settings = get_settings()
    # FastAPI is hardcoded in app.py; only toggle the auto-instrumentation fields.
    # Neo4j has no PyPI instrumentation package.
    for field in (
        "otel_instrument_httpx",
        "otel_instrument_asyncpg",
        "otel_instrument_redis",
        "otel_instrument_celery",
    ):
        monkeypatch.setattr(settings, field, True)


def test_instrument_all_is_idempotent(enable_instrumentation):
    """Calling instrument_all twice should not double-instrument."""
    from emerald.core.tracing_instrumentation import instrument_all

    instrument_all()
    instrument_all()  # Should be a no-op

    tracer = trace.get_tracer(__name__)
    with tracer.start_as_current_span("test"):
        pass


def test_manual_span_recorded(memory_exporter, enable_instrumentation):
    """Manual spans should appear in the in-memory exporter."""
    from emerald.core.tracing_instrumentation import instrument_all

    instrument_all()

    tracer = trace.get_tracer(__name__)
    with tracer.start_as_current_span("manual-span"):
        pass

    spans = memory_exporter.get_finished_spans()
    span_names = [s.name for s in spans]
    assert "manual-span" in span_names


def test_log_includes_trace_id(enable_instrumentation):
    """structlog output should include trace_id when in a span context."""
    import logging
    import io
    from emerald.core.logging import configure_logging, _add_trace_context

    # Capture log output
    import structlog

    output = io.StringIO()
    structlog.configure(
        processors=[
            _add_trace_context,
            structlog.processors.JSONRenderer(),
        ],
        wrapper_class=structlog.make_filtering_bound_logger("INFO"),
        logger_factory=structlog.PrintLoggerFactory(file=output),
    )

    tracer = trace.get_tracer(__name__)
    log = structlog.get_logger(__name__)

    with tracer.start_as_current_span("trace-test"):
        log.info("inside.span")

    log_output = output.getvalue()
    assert "trace_id" in log_output
    assert "span_id" in log_output
```

- [ ] **Step 6: 运行测试**

Run: `pytest tests/tracing/test_otel_integration.py -v`
Expected: 所有测试 PASS

### A4.6 编写可观测性文档

**Files:**
- Create: `docs/deployment/observability.md`

- [ ] **Step 7: 写 OpenTelemetry collector 部署文档**

覆盖：
- OpenTelemetry Collector 部署清单
- 接收端配置（OTLP gRPC + HTTP）
- 导出端配置（Jaeger / Tempo / Datadog 任选）
- 服务映射（哪些 span 属于哪个组件）
- 告警规则建议（基于 span 指标）

```markdown
# Emerald 可观测性配置

## OpenTelemetry 架构

```
┌─────────────────┐
│ emerald-api     │──┐
│ (FastAPI+OTEL)  │  │
└─────────────────┘  │   OTLP gRPC (4317)
                     ├──→ ┌──────────────────────┐
┌─────────────────┐  │    │ OTel Collector        │
│ emerald-worker  │──┘    │ (DaemonSet/Deployment)│
│ (Celery+OTEL)   │       └──────────────────────┘
└─────────────────┘              │
                                 ├──→ Jaeger / Tempo (traces)
┌─────────────────┐              ├──→ Prometheus (metrics)
│ emerald-beat    │──────────────┤
│ (Celery Beat)   │              └──→ Loki / ES (logs)
└─────────────────┘
```

## Collector 部署

详见 `k8s/otel-collector.yaml`（将在 M2 创建）。

最小配置示例：

```yaml
receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

exporters:
  prometheusremotewrite:
    endpoint: http://prometheus:9090/api/v1/write
  otlp/jaeger:
    endpoint: jaeger:4317
    tls:
      insecure: true

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlp/jaeger]
    metrics:
      receivers: [otlp]
      exporters: [prometheusremotewrite]
```

## Trace 服务映射

| Span name pattern | 组件 | 关键属性 |
|---|---|---|
| `POST /v1/memories` | emerald-api | entity_id, memory_type |
| `pipeline.extract` | emerald-worker | content_type |
| `pipeline.chunk` | emerald-worker | chunk_count |
| `pipeline.embed` | emerald-worker | embedding_dim |
| `pipeline.index` | emerald-worker | memory_ids |
| `search.expand_relationships` | emerald-api | expansion_count |
| `graph.get_related_memories` | emerald-api | rel_types |

## 告警规则建议

```yaml
# alerts.yaml
- alert: EmeraldAPILatencyHigh
  expr: histogram_quantile(0.99, http_server_request_duration_seconds_bucket{service="emerald-api"}) > 1
  for: 5m
  labels:
    severity: warning
  annotations:
    summary: "Emerald API P99 > 1s"

- alert: EmeraldPipelineFailureRate
  expr: rate(emerald_pipeline_jobs_total{status="failed"}[5m]) > 0.05
  for: 5m
  labels:
    severity: critical
```

## 本地开发（无需 collector）

设置 `OTEL_CONSOLE_EXPORTER=true` 环境变量，spans 直接打印到 stdout：

```bash
export OTEL_CONSOLE_EXPORTER=true
uvicorn emerald.api.app:app --reload
# 现在每次请求会看到 spans 输出
```
```

### A4.7 A4 验证

- [ ] **Step 8: 运行完整测试套件确保无回归**

```bash
pytest tests/ -x -q --ignore=tests/docker
```

Expected: 601 + 新增 tracing tests 通过

- [ ] **Step 9: 本地启动验证 spans 出现在 stdout**

```bash
export OTEL_CONSOLE_EXPORTER=true
uvicorn emerald.api.app:app &
sleep 3
curl http://localhost:8000/v1/health
# 应该在 stdout 看到 spans
kill %1
```

- [ ] **Step 10: 提交 A4**

```bash
git add emerald/core/tracing.py emerald/core/tracing_instrumentation.py \
        emerald/core/logging.py emerald/config.py emerald/api/app.py \
        tests/tracing/ docs/deployment/observability.md
git commit -m "feat(otel): auto-instrumentation + log correlation

- Add tracing_instrumentation.py with FastAPI/httpx/asyncpg/neo4j/redis/celery hooks
- Inject trace_id and span_id into structlog output
- Extend config with otel_instrument_* toggles
- Mount instrumentation in app lifespan
- Add tests/tracing/test_otel_integration.py (in-memory exporter assertions)
- Add docs/deployment/observability.md with collector deployment + alerts"
```

---

## B0: 真实 LLM 嵌入基准跑分 + 报告

**目标：** 用真实 DeepSeek/OpenAI 嵌入跑 `scripts/run_benchmarks.py --real`，生成可发布的对比报告。

**前置：** 需要 OPENAI_API_KEY 或 DEEPSEEK_API_KEY 环境变量。

**Files:**
- Create: `docs/benchmarks/README.md`
- Create: `docs/benchmarks/2026-06-21-real-llm-results.md`
- Create: `scripts/run_real_benchmarks.sh`

### B0.1 准备跑分脚本

**Files:**
- Create: `scripts/run_real_benchmarks.sh`
- Create: `scripts/benchmark_to_markdown.py`

- [ ] **Step 1: 写 Markdown 后处理器**

**注：** `scripts/run_benchmarks.py` v2 的实际 CLI 标志只有 `--real`/`--mock`/`--llm`/`--json`，**没有** `--output` 或 `--markdown-output`。脚本会自动生成带时间戳的 JSON 到 `reports/benchmark-YYYYMMDD-HHMMSS.json`。本计划新增一个独立的 JSON → Markdown 后处理器。

```python
#!/usr/bin/env python3
"""Convert benchmark JSON report to Markdown summary.

Usage:
    python scripts/benchmark_to_markdown.py <input.json> <output.md>
"""
from __future__ import annotations

import json
import sys
from datetime import datetime
from pathlib import Path


def render(report: dict) -> str:
    lines: list[str] = []
    summary = report.get("summary", {})
    cfg = report.get("config", {})

    lines.append(f"# Emerald Benchmark Report\n")
    lines.append(f"**Generated:** {report.get('timestamp', 'unknown')}\n")
    lines.append(f"**Config:** embeddings={cfg.get('embeddings', '?')}, "
                 f"embedding_dim={cfg.get('embedding_dim', '?')}, "
                 f"llm_relationships={cfg.get('llm_relationships', False)}\n")
    lines.append("## Summary\n")
    lines.append(f"- **Pass rate:** {summary.get('passed', 0)}/{summary.get('total', 0)} "
                 f"= {summary.get('pass_rate', 0):.1%}")
    lines.append(f"- **Aggregate score:** {summary.get('aggregate_score', 0):.3f}\n")
    lines.append("## Per-benchmark Results\n")
    lines.append("| Benchmark | Status | Key metric |")
    lines.append("|---|---|---|")

    for r in report.get("results", []):
        metrics = r.get("metrics", {})
        # Pick the most meaningful metric per benchmark
        for key in ("accuracy", "classification_accuracy", "update_detection_rate",
                    "search_accuracy", "coverage", "overall_accuracy",
                    "keep_rate", "forget_rate", "precision@1", "recall@5", "mrr"):
            if key in metrics:
                key_metric = f"{key}={metrics[key]:.3f}"
                break
        else:
            key_metric = "(no metric)"
        passed = "✅" if r.get("passed") else "❌"
        lines.append(f"| {r.get('name', '?')} | {passed} | {key_metric} |")

    lines.append("\n## Details\n")
    for r in report.get("results", []):
        lines.append(f"### {r.get('name', '?')}")
        lines.append(f"- **Aligns with:** {r.get('aligns_with', '?')}")
        lines.append(f"- **Description:** {r.get('description', '?')}")
        metrics = r.get("metrics", {})
        for k, v in metrics.items():
            if isinstance(v, float):
                lines.append(f"- {k}: {v:.3f}")
            else:
                lines.append(f"- {k}: {v}")
        lines.append("")
    return "\n".join(lines) + "\n"


def main() -> int:
    if len(sys.argv) != 3:
        print(f"Usage: {sys.argv[0]} <input.json> <output.md>", file=sys.stderr)
        return 1
    src = Path(sys.argv[1])
    dst = Path(sys.argv[2])
    report = json.loads(src.read_text())
    dst.write_text(render(report))
    print(f"Wrote {dst} ({dst.stat().st_size} bytes)")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: 写真实嵌入跑分脚本（使用实际 CLI flags）**

```bash
#!/usr/bin/env bash
# scripts/run_real_benchmarks.sh — run benchmarks with real LLM embeddings
# Requires OPENAI_API_KEY or DEEPSEEK_API_KEY in environment
set -euo pipefail

if [ -z "${OPENAI_API_KEY:-}" ] && [ -z "${DEEPSEEK_API_KEY:-}" ]; then
    echo "ERROR: Neither OPENAI_API_KEY nor DEEPSEEK_API_KEY set" >&2
    echo "Export one of them and re-run" >&2
    exit 1
fi

REPORTS_DIR="reports"
mkdir -p "$REPORTS_DIR"

# Run 1: real embeddings, deterministic relationships (baseline)
echo "Running benchmarks with real embeddings (no LLM rel classification)..."
python scripts/run_benchmarks.py --real

# Locate newest auto-generated report
LATEST=$(ls -1t "$REPORTS_DIR"/benchmark-*.json | head -1)
echo "Latest report: $LATEST"

# Convert to Markdown
python scripts/benchmark_to_markdown.py \
    "$LATEST" \
    "docs/benchmarks/2026-06-21-real-llm-results.md"

# Run 2: real embeddings + LLM relationship classification (if DeepSeek available)
if [ -n "${DEEPSEEK_API_KEY:-}" ]; then
    echo "Running with LLM relationship classification (DeepSeek)..."
    python scripts/run_benchmarks.py --real --llm
    LATEST_LLM=$(ls -1t "$REPORTS_DIR"/benchmark-*.json | head -1)
    python scripts/benchmark_to_markdown.py \
        "$LATEST_LLM" \
        "docs/benchmarks/2026-06-21-real-llm-deepseek-results.md"
fi

echo "Done. Reports:"
ls -la "$REPORTS_DIR"/benchmark-*.json
ls -la docs/benchmarks/*.md
```

```bash
chmod +x scripts/run_real_benchmarks.sh
```

- [ ] **Step 3: 验证后处理器在小报告上工作**

创建一个小测试 JSON 并验证输出：

```bash
# Quick smoke test: render a stub report
cat > /tmp/stub.json <<'EOF'
{
  "timestamp": "2026-06-21T00:00:00",
  "config": {"embeddings": "stub", "embedding_dim": 1536, "llm_relationships": false},
  "summary": {"passed": 1, "total": 1, "pass_rate": 1.0, "aggregate_score": 1.0},
  "results": [{"name": "Smoke", "metrics": {"accuracy": 1.0}, "passed": true}]
}
EOF
python scripts/benchmark_to_markdown.py /tmp/stub.json /tmp/stub.md
grep -q "Pass rate" /tmp/stub.md && echo "✓ Renderer works"
```

### B0.2 跑分执行

**Files:**
- Create: `docs/benchmarks/2026-06-21-real-llm-results.md` (auto-generated)

- [ ] **Step 2: 设置 API key 并执行真实跑分**

```bash
export OPENAI_API_KEY="sk-..."
./scripts/run_real_benchmarks.sh
```

Expected: 
- 6 维度 benchmark 跑完
- JSON 报告保存到 `reports/benchmark-real-YYYYMMDD-HHMMSS.json`
- Markdown 报告保存到 `docs/benchmarks/2026-06-21-real-llm-results.md`

**注意：** 此步骤需要在有 API key 的环境中运行。如果无 API key，跳过步骤 2 但需在 README 中标注「需要在有 OPENAI_API_KEY 的环境手动运行」。

### B0.3 撰写基准 README

**Files:**
- Create: `docs/benchmarks/README.md`

- [ ] **Step 3: 写基准说明文档**

```markdown
# Emerald Memory Benchmarks

## 概览

Emerald 基准测试套件评估记忆系统的 6 个核心维度，对齐 LongMemEval、LoCoMo、ConvoMem 三大公开基准。

## 运行方式

### Mock 嵌入（CI / 快速验证）
```bash
python scripts/run_benchmarks.py
# 6 维度全部跑完，~2 分钟
# 输出 JSON 到 reports/benchmark-*.json
```

### 真实嵌入（需要 API key）
```bash
export OPENAI_API_KEY="sk-..."
./scripts/run_real_benchmarks.sh
# 使用 OpenAI text-embedding-3-small (1536 dim)
# 跑完 ~10 分钟，输出 Markdown 报告到 docs/benchmarks/
```

### 真实 LLM 关系分类
```bash
export DEEPSEEK_API_KEY="sk-..."
python scripts/run_benchmarks.py --real --llm
```

## 6 个维度

| 维度 | 对齐基准 | 数据规模 |
|---|---|---|
| Fact Recall | LongMemEval Info Extraction | 100 facts → 30 queries |
| Temporal Updates | LongMemEval Knowledge Updates | 10 timelines × 5 steps |
| Relationship Class | 自定义 | 18 pairs |
| Profile Accuracy | LoCoMo persona | 20 facts |
| Distractor Resist | LoCoMo/ConvoMem | 5 targets + 50 noise |
| Forgetting Correct | 自定义 | 10 mixed facts |

## 已发布的报告

- [2026-06-15 mock 嵌入跑分](./2026-06-15-mock-results.md)（基线）
- [2026-06-21 真实 LLM 跑分](./2026-06-21-real-llm-results.md)（首次真实嵌入）

## 已知局限

- Mock 嵌入无语义能力，Fact Recall / Distractor 维度得分偏低（~10-20%）
- 真实 LLM 跑分需要付费 API key
- 基准不覆盖：多模态摄入性能、跨语言能力、图谱规模 > 100k 节点
```

### B0.4 B0 验证

- [ ] **Step 4: 验证报告文件存在并格式正确**

```bash
test -f docs/benchmarks/2026-06-21-real-llm-results.md && echo "✓ Report file exists"
test -f reports/benchmark-real-*.json && echo "✓ JSON report exists"
```

- [ ] **Step 5: 提交 B0**

```bash
git add scripts/run_real_benchmarks.sh docs/benchmarks/
git commit -m "feat(benchmarks): real LLM embedding run + report infrastructure

- scripts/run_real_benchmarks.sh: one-shot real embedding runner
- docs/benchmarks/README.md: explain 6 dimensions + how to run
- docs/benchmarks/2026-06-21-real-llm-results.md: first real LLM report

Compared to mock baseline (0.667 aggregate), real LLM results expected
to improve Fact Recall and Distractor Resist significantly."
```

---

## D1: 基准 CI 自动化（GitHub Actions）

**目标：** 每次 PR 和每周一自动跑分，结果作为 GitHub Actions artifact 保存；分数下降 > 10% 触发 **PR check failure**（不是 PR 评论）。

**前置：** B0 已完成（基准脚本可跑）。

**Files:**
- Create: `.github/workflows/benchmark.yml`
- Create: `.github/workflows/benchmark-compare.py` (回归检测脚本 — 退出码 1 触发 PR check failure)
- Create: `tests/benchmarks/test_benchmark_runner.py`

### D1.1 GitHub Actions workflow

**Files:**
- Create: `.github/workflows/benchmark.yml`

- [ ] **Step 1: 写 benchmark CI workflow**

```yaml
# .github/workflows/benchmark.yml
name: Memory Benchmarks

on:
  # 每周一 UTC 02:00 跑完整基准
  schedule:
    - cron: '0 2 * * 1'
  # PR 改动核心模块时跑快速 smoke 基准
  pull_request:
    paths:
      - 'emerald/core/**'
      - 'emerald/pipeline/**'
      - 'scripts/run_benchmarks.py'

jobs:
  benchmark-mock:
    name: Mock Embedding Benchmarks
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Run benchmarks (mock embeddings)
        run: |
          mkdir -p reports
          python scripts/run_benchmarks.py --mock
          # Script auto-saves to reports/benchmark-YYYYMMDD-HHMMSS.json
          # Symlink for downstream comparison
          LATEST=$(ls -1t reports/benchmark-*.json | head -1)
          ln -sf "$(basename "$LATEST")" reports/benchmark-pr.json

      - name: Upload report artifact
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-report
          path: reports/benchmark-*.json
          retention-days: 30

      - name: Compare to baseline (PR only)
        if: github.event_name == 'pull_request'
        env:
          BASELINE_PATH: reports/baseline.json
        run: |
          # Use latest weekly report as baseline
          if [ -f "$BASELINE_PATH" ]; then
            python .github/workflows/benchmark-compare.py \
              reports/benchmark-pr.json \
              $BASELINE_PATH
          else
            echo "No baseline found; skipping comparison"
          fi

  benchmark-real:
    name: Real LLM Benchmarks (weekly only)
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule'
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements-dev.txt

      - name: Run real benchmarks
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          mkdir -p reports
          ./scripts/run_real_benchmarks.sh
          # Save as latest baseline (avoid glob race: pick newest file explicitly)
          LATEST=$(ls -1t reports/benchmark-*.json | head -1)
          cp "$LATEST" reports/baseline.json

      - name: Upload real report
        uses: actions/upload-artifact@v4
        with:
          name: real-benchmark-report
          path: reports/benchmark-real-*.json
          retention-days: 90
```

### D1.2 PR 比较脚本

**Files:**
- Create: `.github/workflows/benchmark-compare.py`

- [ ] **Step 2: 写 PR 比较脚本（修正 JSON 结构）**

**注：** 实际 `reports/benchmark-*.json` 结构为：
```json
{
  "summary": {"pass_rate": 0.667, "aggregate_score": 0.684, "passed": 4, "total": 6},
  "results": [
    {"name": "...", "metrics": {"<key>": <value>, ...}, "passed": true}
  ]
}
```
即 `summary` 在**顶层**（不是每个 result 内）；`result["metrics"]` 是扁平字典（不是嵌套 summary）。原计划 v1 错误地把 summary 访问放在 result 内部，导致 metrics 静默为 0。

```python
#!/usr/bin/env python3
"""Compare current benchmark results to baseline.

Exits 1 if any aggregate metric drops > 10% (triggers CI failure).

Usage:
    python benchmark-compare.py <current.json> <baseline.json>
"""
from __future__ import annotations

import json
import sys
from pathlib import Path

REGRESSION_THRESHOLD = 0.10  # 10%

# Accuracy-like metric keys we track per benchmark
METRIC_KEYS = (
    "accuracy", "classification_accuracy", "update_detection_rate",
    "search_accuracy", "coverage", "overall_accuracy",
    "keep_rate", "forget_rate", "precision@1", "recall@5", "mrr",
)


def load_report(path: Path) -> dict:
    return json.loads(path.read_text())


def extract_metrics(report: dict) -> dict[str, float]:
    """Extract {benchmark_name: primary_metric_value} from a report."""
    metrics: dict[str, float] = {}
    # Top-level summary
    summary = report.get("summary", {})
    if "pass_rate" in summary:
        metrics["__aggregate__"] = summary["pass_rate"]
    if "aggregate_score" in summary:
        metrics["__aggregate_score__"] = summary["aggregate_score"]
    # Per-benchmark primary metric
    for r in report.get("results", []):
        name = r.get("name")
        if not name:
            continue
        for key in METRIC_KEYS:
            if key in r.get("metrics", {}):
                metrics[name] = r["metrics"][key]
                break
    return metrics


def main() -> int:
    if len(sys.argv) != 3:
        print(f"Usage: {sys.argv[0]} <current.json> <baseline.json>", file=sys.stderr)
        return 1

    current = load_report(Path(sys.argv[1]))
    baseline = load_report(Path(sys.argv[2]))

    cur = extract_metrics(current)
    base = extract_metrics(baseline)

    regressions: list[str] = []
    improvements: list[str] = []

    for name, cur_val in cur.items():
        if name not in base:
            continue
        base_val = base[name]
        if base_val == 0:
            continue
        delta = (cur_val - base_val) / base_val
        if delta < -REGRESSION_THRESHOLD:
            regressions.append(f"  ❌ {name}: {base_val:.3f} → {cur_val:.3f} ({delta:+.1%})")
        elif delta > REGRESSION_THRESHOLD:
            improvements.append(f"  ✅ {name}: {base_val:.3f} → {cur_val:.3f} ({delta:+.1%})")

    print("=" * 60)
    print("Benchmark Comparison")
    print("=" * 60)

    if regressions:
        print("\nRegressions (> 10% drop):")
        for r in regressions:
            print(r)

    if improvements:
        print("\nImprovements (> 10% gain):")
        for i in improvements:
            print(i)

    if not regressions and not improvements:
        print("\nNo significant changes (within ±10%)")

    print("=" * 60)

    return 1 if regressions else 0


if __name__ == "__main__":
    sys.exit(main())
```

```bash
chmod +x .github/workflows/benchmark-compare.py
```

### D1.3 测试比较脚本

**Files:**
- Create: `tests/benchmarks/test_benchmark_runner.py`

- [ ] **Step 3: 写比较脚本单元测试（使用真实 JSON 结构）**

**重要修复：** v1 计划用错误的 JSON 形状（`result["summary"]["pass_rate"]`）做测试 fixtures，导致测试通过但实际执行静默失败。下面的 fixtures 使用真实结构（顶层 summary + 扁平 metrics dict）。

```python
"""Tests for benchmark CI comparison logic.

Fixtures match the REAL structure of `reports/benchmark-*.json`:
    {
      "summary": {"pass_rate": ..., "aggregate_score": ..., ...},
      "results": [{"name": ..., "metrics": {<flat dict>}, ...}]
    }
"""
from __future__ import annotations

import json
import subprocess
import sys
from pathlib import Path

COMPARE_SCRIPT = Path(__file__).parent.parent.parent / ".github" / "workflows" / "benchmark-compare.py"


def _write_realistic_report(path: Path, *, pass_rate: float,
                            fact_recall_precision: float = 0.13) -> None:
    """Write a JSON file matching the real benchmark output structure."""
    report = {
        "version": "2.0",
        "timestamp": "2026-06-21T00:00:00+00:00",
        "config": {"embeddings": "mock", "embedding_dim": 1536, "llm_relationships": False},
        "results": [
            {
                "name": "Fact Recall",
                "metrics": {
                    "precision@1": fact_recall_precision,
                    "recall@5": fact_recall_precision,
                    "mrr": fact_recall_precision,
                },
                "passed": fact_recall_precision > 0.10,
            },
            {
                "name": "Relationship Classification",
                "metrics": {"classification_accuracy": 0.94},
                "passed": True,
            },
        ],
        "summary": {
            "passed": int(pass_rate * 6),
            "total": 6,
            "pass_rate": pass_rate,
            "aggregate_score": pass_rate,
        },
    }
    path.write_text(json.dumps(report))


def test_no_regression_returns_zero(tmp_path: Path) -> None:
    current = tmp_path / "current.json"
    baseline = tmp_path / "baseline.json"
    _write_realistic_report(current, pass_rate=0.7)
    _write_realistic_report(baseline, pass_rate=0.7)

    result = subprocess.run(
        [sys.executable, str(COMPARE_SCRIPT), str(current), str(baseline)],
        capture_output=True,
        text=True,
    )
    assert result.returncode == 0, f"unexpected exit {result.returncode}: {result.stdout}\n{result.stderr}"
    assert "No significant changes" in result.stdout


def test_aggregate_regression_returns_nonzero(tmp_path: Path) -> None:
    """A drop in top-level pass_rate (0.70 → 0.50, ~29%) must fail CI."""
    current = tmp_path / "current.json"
    baseline = tmp_path / "baseline.json"
    _write_realistic_report(current, pass_rate=0.50)
    _write_realistic_report(baseline, pass_rate=0.70)

    result = subprocess.run(
        [sys.executable, str(COMPARE_SCRIPT), str(current), str(baseline)],
        capture_output=True,
        text=True,
    )
    assert result.returncode == 1
    assert "Regressions" in result.stdout
    assert "__aggregate__" in result.stdout


def test_per_benchmark_regression_detected(tmp_path: Path) -> None:
    """A per-benchmark metric drop (Fact Recall 0.50 → 0.10, 80% drop) must fail."""
    current = tmp_path / "current.json"
    baseline = tmp_path / "baseline.json"
    _write_realistic_report(current, pass_rate=0.7, fact_recall_precision=0.10)
    _write_realistic_report(baseline, pass_rate=0.7, fact_recall_precision=0.50)

    result = subprocess.run(
        [sys.executable, str(COMPARE_SCRIPT), str(current), str(baseline)],
        capture_output=True,
        text=True,
    )
    assert result.returncode == 1
    assert "Fact Recall" in result.stdout


def test_improvement_is_reported(tmp_path: Path) -> None:
    current = tmp_path / "current.json"
    baseline = tmp_path / "baseline.json"
    _write_realistic_report(current, pass_rate=0.85)
    _write_realistic_report(baseline, pass_rate=0.70)

    result = subprocess.run(
        [sys.executable, str(COMPARE_SCRIPT), str(current), str(baseline)],
        capture_output=True,
        text=True,
    )
    assert result.returncode == 0
    assert "Improvements" in result.stdout


def test_missing_args_errors() -> None:
    result = subprocess.run(
        [sys.executable, str(COMPARE_SCRIPT)],
        capture_output=True,
        text=True,
    )
    assert result.returncode == 1
    assert "Usage" in result.stderr
```

- [ ] **Step 4: 运行测试**

Run: `pytest tests/benchmarks/test_benchmark_runner.py -v`
Expected: 5 tests PASS

### D1.4 D1 验证

- [ ] **Step 5: 本地完整跑分验证 workflow YAML 语法**

```bash
# 使用 act 或 yamllint 验证
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/benchmark.yml'))" && echo "✓ YAML valid"

# Smoke test: 用 mock 嵌入跑一次
mkdir -p /tmp/bench-test-reports
cd /tmp/bench-test-reports && python /Users/nicholasl/Documents/build-whatever/Emerald/scripts/run_benchmarks.py --mock 2>&1 | tail -5
LATEST=$(ls -1t /tmp/bench-test-reports/benchmark-*.json 2>/dev/null | head -1)
if [ -n "$LATEST" ] && [ -f "$LATEST" ]; then
    echo "✓ Benchmark runs (output: $LATEST)"
else
    echo "ERROR: Benchmark did not produce a JSON report" >&2
    exit 1
fi
```

- [ ] **Step 6: 提交 D1**

```bash
git add .github/workflows/benchmark.yml .github/workflows/benchmark-compare.py tests/benchmarks/test_benchmark_runner.py
git commit -m "feat(ci): automated benchmark workflow + regression detection

- .github/workflows/benchmark.yml: weekly + PR-triggered runs
- benchmark-compare.py: exits 1 on > 10% regression
- tests/benchmarks/test_benchmark_runner.py: 4 tests for compare logic

Weekly job uses OPENAI_API_KEY secret for real LLM benchmarks;
PR job uses mock embeddings for fast feedback (< 5 min)."
```

---

## M1 验证清单（所有工作项完成后）

### 必须全部满足才能合并到 main

- [ ] **V1: 所有测试通过（含 A1 + A4 + D1 新增测试）**

```bash
pytest tests/ -q
```

Expected: ≥ 613 tests passed (601 + 4 A1 + 3 A4 + 5 D1 = 613)

- [ ] **V2: Production Docker 镜像 < 1.2GB（默认含 ffmpeg）/ < 800MB（无 ffmpeg）**

```bash
./scripts/build_prod_image.sh emerald:m1-verify
```

Expected: 输出 `Image size: <1.2GB`（或 <800MB 如 `BUILD_NO_FFMPEG=true`），健康检查通过

- [ ] **V3: K8s manifests 通过静态验证**

```bash
./scripts/verify_k8s_manifests.sh
```

Expected: 输出 "All K8s manifests validated"

- [ ] **V4: OpenTelemetry 集成测试通过**

```bash
pytest tests/tracing/ -v
```

Expected: 所有 tracing 测试 PASS

- [ ] **V5: 真实 LLM 基准报告存在（如果执行了 B0 步骤 2）**

```bash
test -f docs/benchmarks/2026-06-21-real-llm-results.md
```

Expected: 文件存在（无 API key 时跳过）

- [ ] **V6: GitHub Actions workflow YAML 语法正确**

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/benchmark.yml'))"
```

Expected: 无错误

- [ ] **V7: 无未提交的变更**

```bash
git status
```

Expected: working tree clean

### M1 发布准备

- [ ] **V8: 更新 CHANGELOG.md**

```markdown
## [0.4.0] — 2026-06-21

### Added

#### A1: Dockerfile production stage 独立 pip install
- 拆分 requirements.txt 为 requirements-prod.txt（运行时子集）
- Production 镜像不再从 development 阶段拷贝 site-packages
- 镜像体积从 ~1.5GB 减小到 < 1.2GB（减小 20%，含 ffmpeg）或 < 800MB（减小 47%，不含 ffmpeg）
- 添加 .dockerignore + build_prod_image.sh + tests/docker/

#### A2: K8s manifest 验证 + 灾备演练
- scripts/verify_k8s_manifests.sh: 静态检查 YAML + namespace 一致性
- scripts/k8s_disaster_recovery.sh: Postgres/Neo4j/Redis 备份
- docs/deployment/k8s-runbook.md: 完整运维手册

#### A4: OpenTelemetry 自动 instrumentation
- tracing_instrumentation.py: FastAPI/httpx/asyncpg/neo4j/redis/celery 自动 hook
- structlog 日志自动注入 trace_id 和 span_id
- docs/deployment/observability.md: Collector 部署 + 告警规则

#### B0: 真实 LLM 嵌入基准
- scripts/run_real_benchmarks.sh: 一键跑分
- docs/benchmarks/2026-06-21-real-llm-results.md: 首次真实嵌入报告

#### D1: 基准 CI 自动化
- .github/workflows/benchmark.yml: 每周 + PR 自动跑分
- benchmark-compare.py: 回归检测（> 10% 下降触发 CI 失败）
```

- [ ] **V9: 提交 CHANGELOG**

```bash
git add CHANGELOG.md
git commit -m "docs: CHANGELOG for v0.4.0"
```

- [ ] **V10: 打 tag v0.4.0**

```bash
git tag -a v0.4.0 -m "v0.4.0: Production deployment + verification infrastructure"
git push origin v0.4.0
```

---

## 完成时间估算（单人顺序执行）

| 工作项 | 工作量 |
|---|---|
| A1 | 1-2 天 |
| A2 | 2-3 天（runbook 占大头） |
| A4 | 3-4 天（auto-instrumentation 配置 + 日志关联 + 文档） |
| B0 | 1 天（脚本 + 跑分 + 报告） |
| D1 | 2 天（workflow + compare 脚本 + 测试） |
| M1 验证 + 发布 | 1 天 |
| **总计** | **~10-13 天（约 2-3 周）** |

---

## 风险与备选方案

| 风险 | 缓解 |
|---|---|
| A4 自动 instrumentation 与某些库版本冲突 | 分阶段启用，每个 instrumentation 独立测试 |
| B0 需要付费 API key | 在 README 中标注，提供 mock 替代路径 |
| GitHub Actions runner 跑真实基准超过 10 分钟 | 改用 self-hosted runner，或仅 weekly 跑真实基准 |
| Docker 构建在 CI 中过慢 | 使用 BuildKit 缓存（`docker/build-push-action@v5`） |

---

## 下一步（M2 准备）

完成 M1 后，**不要立即开始 M2**。先做：

1. **用户反馈收集**：M1 的 tracing 和 benchmark 报告对实际开发体验的改善程度
2. **指标验证**：北极星指标是否按预期演进
3. **M2 spec 撰写**：按 writing-plans skill 为 M2 创建独立实施计划

M2 的 5 个工作项（v0.5.0）：
- A3 v2 API 实质化
- B1 Cross-encoder 重排序
- B2 LLM-first 关系推断
- C1 TypeScript SDK v1
- A6 安全审计

---

*本计划依据 docs/roadmap.md §四 M1 制定，于 2026-06-21 起草。*
