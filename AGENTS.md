# AGENTS.md — Emerald

## 项目定位

Emerald 是面向 AI Agent 的记忆与上下文基础设施。它通过知识图谱架构（事实建立在其他事实之上）提供长期和短期记忆，具备自动提取、时序追踪、上下文更新和智能遗忘能力。它不是文档存储系统，而是一个活的、不断演化的记忆系统。

对标项目：Supermemory（supermemory.ai）——在 LongMemEval、LoCoMo 和 ConvoMem 三项基准上均达到最优水平。

---

## 架构原则

以下为不可变更的设计约束。每个功能、每个 PR、每次重构都必须遵循。

### 1. 记忆 ≠ RAG

这是最根本的区分。RAG 检索的是静态文档块——所有人获得相同结果。记忆则提取和追踪*关于实体的、随时间变化的事实*，理解时序有效性、矛盾关系以及个性化。Emerald 默认同时运行两者，但它们是根本不同的系统。

**切勿**混淆二者。记忆系统不是加了个用户 ID 的向量数据库。

### 2. 图谱优先，而非向量优先

Emerald 的主要数据模型是知识图谱：记忆（事实）通过关系（更新、扩展、推导）彼此连接。向量/嵌入是优化层，不是核心模型。图谱使时序推理、矛盾解决和推导成为可能——这些是纯向量搜索无法做到的。

### 3. 同一个上下文池

记忆、用户画像和 RAG 文档在使用相同实体 ID 时，都从同一个上下文池中获取数据。它们是同一个图谱的三种视图。不要建立信息孤岛。

### 4. 全自动，非手动

用户和开发者永远不需要手动：
- 定义记忆之间的关系
- 标记记忆类型
- 清理过期记忆
- 解决矛盾
- 选择分块策略

系统自动推断这一切。API 设计应体现这一点——最小化、声明式、高层次的接口。

### 5. 用户画像是默认上下文

在任何搜索发生之前，用户画像（静态 + 动态事实）应该是首先注入 Agent 上下文的内容。它回答了「这个用户是谁？」，Agent 无需主动询问。画像始终最新，始终在约 50ms 内可用。

### 6. 时序完整性

每个事实都有时序上下文：何时为真、何时更新、何时过期（如适用）。系统必须处理：
- 知识更新（新事实取代旧事实）
- 基于时间的遗忘（「我明天有考试」→ 过了明天就遗忘）
- 矛盾解决（与旧事实冲突的新事实）
- 情节衰减（不重要的事件随时间推移失去相关性）

### 7. 按类型分块

不同内容类型需要不同的分块策略：
- 代码 → AST 感知（函数、类保持完整）
- Markdown → 标题层级
- PDF → 语义章节
- 对话文本 → 逻辑轮次/用户消息

一刀切的字符分割不是目标。

### 8. 提取，而非存储

当内容被摄入时，Emerald 必须*提取含义*——而不仅仅是存储原始字节。每段内容经过：提取 → 分块 → 嵌入 → 关系构建。原始输入可以被丢弃或归档，但图谱中提取出的事实才是真正的来源。

---

## 关键设计决策

### 为什么用图谱而非纯向量存储？

向量能发现相似性。图谱能理解关系、时间和因果。用户换工作不是一个「相似」的记忆——它是一个使之前事实失效的更新。向量搜索无法表达这一点。图谱可以。

### 为什么只有三种关系类型？

- **更新：** 时序演进。追踪哪些是当前状态，哪些是历史。
- **扩展：** 上下文丰富。两个事实都保持有效。
- **推导：** 推理。展示用户未明确陈述的洞察。

这三种类型覆盖了事实之间所有有意义的连接。添加更多关系类型应该面临很高的论证门槛。

### 为什么需要用户画像而非仅靠搜索记忆？

纯搜索记忆要求 Agent 知道该问什么。画像让 Agent 无需任何查询就知道用户是谁。这显著减少了延迟，提升了个性化质量。同时支持「会话开始即注入上下文」的模式——在第一条消息之前就已注入画像。

### 为什么需要自动遗忘？

没有遗忘，每句随意的话都会变成永久记忆。图谱膨胀，噪音累积，检索质量下降。遗忘不是 bug——它是一项特性。系统必须能区分有意义的事实和短暂的闲聊。

---

## 预期代码结构

```
Emerald/
├── emerald/
│   ├── core/           # 记忆引擎、图谱操作、提取、分块
│   ├── api/            # HTTP API 层
│   ├── sdk/            # Python SDK
│   ├── connectors/     # 外部数据源集成
│   └── mcp/            # MCP 服务
├── docs/               # 文档
├── tests/              # 测试
├── AGENTS.md           # 本文件
├── README.md           # 项目概述
└── pyproject.toml      # Python 项目配置
```

---

## 技术指南

### 语言
- 核心引擎：Python（主要），性能关键路径必要时可扩展为 Rust 或 Go。
- SDK：Python 优先。其他语言按需求决定。

### 数据库
- 图谱：图原生数据库（如 Neo4j、SurrealDB 或自研进程内图谱）。
- 向量：支持向量的存储引擎。理想情况下同一数据库同时支持图谱和向量（如 SurrealDB、pgvector、Weaviate）。
- 文档：对象存储（S3 兼容）存放原始文件。

### API 风格
- REST + SDK 封装。如果微服务架构出现，内部服务间通信使用 gRPC。
- 认证使用 API Key。面向消费者的集成使用 OAuth。

### 测试
- 单元测试覆盖提取、分块、关系解析。
- 集成测试覆盖完整管线（摄入 → 图谱 → 检索）。
- 基于标准数据集的记忆基准测试（LongMemEval 风格）。
- 每种关系类型必须有确定性的测试用例。

### 可观测性
- 结构化日志（JSON）。每个管线阶段必须记录：实体 ID、内容类型、耗时、分块数量、关系数量、错误状态。
- 指标：摄入吞吐量、提取延迟、画像获取延迟、搜索延迟、每个实体的关系密度。

---

## API 设计原则

1. **最小接口面。** `add`、`search`、`profile`、`upload` ——四个核心方法。其他都是可选的优化。
2. **实体优先。** 每个操作都限定在某个实体 ID（用户、项目、组织）范围内。不允许全局的、匿名的上下文池。
3. **声明式，非过程式。** 开发者描述他们想要什么，而非如何实现。无需配置分块参数。无需选择嵌入模型。无需管理关系。
4. **开箱即用的默认值。** 默认混合搜索模式。自动检测内容类型。每个参数都有智能默认值。
5. **SDK 一致性。** 各语言 SDK 应有一致的概念性 API。方法名、参数结构和返回类型应一一对应。

---

## 关键概念词汇表

| 术语 | 定义 |
|---|---|
| **实体（Entity）** | 用户、项目、组织或其他任何记忆归属的身份单元。也称实体 ID 或命名空间。 |
| **记忆 / 事实（Memory / Fact）** | 一条被提取的、结构化的、关联到某个实体的知识。具有时序上下文、类型（事实/偏好/情节）以及与其他记忆的关系。 |
| **关系（Relationship）** | 两条记忆之间的有向连接：更新（取代）、扩展（丰富）、推导（推理）。 |
| **知识图谱（Knowledge Graph）** | 由关系连接起来的记忆网络。Emerald 的主要数据模型。 |
| **用户画像（User Profile）** | 一个实体的双层摘要：静态事实（始终相关）和动态事实（近期的、情节性的）。从图谱中计算得出。 |
| **RAG** | 检索增强生成（Retrieval-Augmented Generation）。通过语义/向量搜索进行的无状态文档检索。用于知识库、文档、参考资料。 |
| **混合搜索（Hybrid Search）** | 单次查询同时返回 RAG 结果（文档）和记忆结果（用户特定事实）。默认搜索模式。 |
| **分块（Chunking）** | 在嵌入之前将内容拆分为语义单元的过程。策略取决于内容类型（代码用 AST，Markdown 用标题层级，PDF 用章节）。 |
| **管线（Pipeline）** | 完整的摄入流程：排队 → 提取 → 分块 → 嵌入 → 索引 → 完成。 |
| **遗忘（Forgetting）** | 自动移除或弃用记忆：基于时间的过期、矛盾解决和噪音过滤。 |
| **连接器（Connector）** | 将外部数据源（Google Drive、Gmail、Notion、GitHub）通过 Webhook 同步到 Emerald 的集成模块。 |

---

## AI Agent 开发指南

在对此代码库进行开发时：

1. **在进行任何修改前，先阅读上述架构原则。** 它们是不可协商的。
2. **记忆 ≠ RAG。** 如果某个修改建议混淆了二者，请重新思考。
3. **图谱操作必须是原子的。** 一个事实更新取代旧事实时，必须在单次事务中设置 `isLatest` 和创建 Update 关系。
4. **每次提取必须优雅处理失败。** 一个损坏的 PDF 不应阻塞整个管线。提取失败应记录错误并继续处理。
5. **画像必须快速。** 静态 + 动态画像接口必须在 100ms 内返回。积极使用缓存。在摄入时重新计算，而非在读取时计算。
6. **测试必须覆盖时序场景。** 不仅仅是「查找相似文本」——要测试「当事实 B 到达时，事实 A 变为过时」。
7. **禁止 API 泄漏。** SDK 不得暴露内部图谱操作。公共 API 仅限 `add`、`search`、`profile`、`upload`。
8. **v1 上线前必须支持 README 中列出的所有内容类型。** 不要发布部分覆盖的提取能力。
9. **在宣告工作完成前，运行完整测试套件并验证。**
10. **绝不提交密钥或 Token。** 使用环境变量。`.env.example` 文件为所需配置的权威参考。
