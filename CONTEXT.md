# Emerald Domain Context

面向 AI Agent 的记忆与上下文基础设施：从对话与内容中自动提取关于实体的、随时间演化的事实，以知识图谱为数据模型组织它们，并在恰当的时机提供最小且最相关的上下文。它不是文档存储系统，而是一个活的、不断演化的记忆系统。

## 记忆与检索

**记忆（Memory / Fact）**:
一条被提取的、结构化的、关联到某个实体的知识。具有时序上下文、类型（事实/偏好/情节）以及与其他记忆的关系。图谱中的基本单元。
_Avoid_: 文档、chunk、记录

**实体（Entity）**:
记忆归属的身份单元（用户、项目、组织）。所有操作都限定在实体范围内；不存在全局、匿名的上下文池。
_Avoid_: 命名空间、租户、user_id

**提及（Mention）**:
一条记忆所谈论的命名事物（人、组织、地点、技术、概念等）。同一事物在一个实体的上下文池内只解析为一个提及，不同表层形式（如「谷歌」「Google」）聚合到同一提及并保留为别名。与实体相对：实体是记忆归属的命名空间，提及是记忆谈论的对象。
_Avoid_: 命名实体（作领域词）、NER 标签、entity（在指提及时）

**知识图谱（Knowledge Graph）**:
由关系连接起来的记忆网络。Emerald 的主要数据模型——向量/嵌入只是优化层。
_Avoid_: 数据库、向量索引

**用户画像（User Profile）**:
一个实体的双层摘要：静态事实（始终相关）与动态事实（近期、情节性）。在任何搜索发生之前注入 Agent 上下文；始终最新。
_Avoid_: 用户模型、persona、profile 配置

**RAG**:
通过语义/向量搜索进行的无状态文档检索。与记忆根本不同：无状态、无时序、无关系、从不遗忘。
_Avoid_: 与记忆混用为同一概念

**混合搜索（Hybrid Search）**：
单次查询同时返回 RAG 结果（文档）与记忆结果（实体特定事实）。默认搜索模式。
_Avoid_: 全文搜索

**实体中心检索（Entity-centric Retrieval）**：
`search(about=...)` —— 按提及（规范形式或提及 id）返回实体上下文池内提及该事物的全部最新记忆，跨所有表层形式；不经向量相似度，不触发 RAG。检索是记忆图谱操作，结果集恰为提及集。
_Avoid_: 关键词搜索、实体查找（非检索语义）

**多跳推理（Multi-hop Reasoning）**：
从种子记忆沿图谱边行走的遍历：一跳 = 共享提及桥（记忆→提及→记忆）或一条关系边（UPDATES/EXTENDS/DERIVES_FROM，双向）。推导事实作为来源参与下一跳（链式）；每跳实体隔离；历史节点仅在沿 UPDATES 被踩到时浮出并标记，从不被穿越；深度默认 0（现状），显式 `search(..., depth=N)` 才多跳，上限 4；每个多跳结果携带路径与跳数。它是 `search` 的深度参数而非独立方法（AGENTS.md 最小接口原则；参数可增可撤、决策可逆——不立 ADR，spec #29）。
_Avoid_: 图查询（作产品概念）、递归检索、traverse API

## 关系与时序

**更新（Update）**:
一种时序关系：新事实取代旧事实，旧事实成为历史。发生时原子地切换当前状态。
_Avoid_: 覆盖、替换（作为领域词）

**扩展（Extends）**:
一种关系：上下文丰富。两条记忆都保持有效。
_Avoid_: 关联

**推导（Derives From）**:
一种关系：推理。展示用户未明确陈述的洞察。
_Avoid_: 推论（作为名词）

**遗忘（Forgetting）**:
自动移除或弃用记忆：基于时间的过期、矛盾解决与噪音过滤。是特性，不是 bug。
_Avoid_: 删除、清理

## 摄入与内容

**管线（Pipeline）**:
完整的摄入流程：排队 → 提取 → 分块 → 嵌入 → 索引 → 完成。
_Avoid_: 处理流程、工作流

**分块（Chunking）**:
在嵌入之前将内容拆分为语义单元；策略取决于内容类型（代码用 AST、Markdown 用标题层级、PDF 用章节、对话按轮次）。
_Avoid_: 切分、字符分割

**数据源绑定（Source Binding）**:
一个外部账户经连接中心授权后在 Emerald 中的绑定：授权关系 + 数据源身份。集成能力（OAuth、同步、webhook）在连接中心侧；Emerald 通过事件驱动摄入外部内容。
_Avoid_: 连接器、集成、插件（连接器一词让位给 hub 侧）

**连接中心（Connection Hub）**:
提供统一外部数据源接入能力的平台：OAuth 授权、凭证管理、同步、动作执行/审计托管。Emerald 不与 provider 直接打交道，只对接连接中心。当前实现为 Totem（内部自托管动作层，v1 upstream = Feishu Docs），**可替换**——任何接入代码只依赖连接中心的抽象接口（ADR-0004；契约见 Totem `docs/standards/consumption-standard.md`）。
_Avoid_: 集成平台、iPaaS（作为领域词）

**空间（Space）**:
实体记忆的命名子集视图，由用户显式创建（产品层意图），不改变上下文池边界。搜索默认全池，空间仅为可选过滤；画像跨空间聚合；系统不自动创建或推断空间。
_Avoid_: 文件夹、标签、集合、分区
